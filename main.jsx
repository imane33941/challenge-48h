import React from "react";
import ReactDOM from "react-dom/client";
import AccueilQuiz from "./accueil_quiz.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AccueilQuiz />
  </React.StrictMode>
);
